import { Col, Row } from "react-bootstrap";

function NoTaskCreated() {
    return (
        <Row>
            <Col>
                <p>There is currently no tasks created.</p>
            </Col>
        </Row>
    );
}

export default NoTaskCreated;